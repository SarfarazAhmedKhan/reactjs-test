import React from "react";
import logo from "../../assets/logo/login-logo.png";
import adminLogo from "../../assets/logo/logo-admin.png";
import {APP_NAME} from "../../constants/constant";

const MainLogo = ({isSideBar = false, className}) => {
    // return <img src={isSideBar ? adminLogo : logo} width="300" alt={APP_NAME} className={className} />;
};

export default MainLogo;
